# Seven Day Price Chart for Token Pairs

This project was created with create-react-app and uses two libraries: axios and recharts

## Running Locally

Clone repo and execute the following commands:

### `npm install`
### `npm start`
